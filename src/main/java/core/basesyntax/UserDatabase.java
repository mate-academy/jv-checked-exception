package core.basesyntax;

public class UserDatabase {
    public void saveUser(String username, String password) {
        System.out.println("User " + username + " was saved to database!!!");
    }
}
